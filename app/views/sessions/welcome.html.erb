<h1>Welcome</h1>
<% if logged_in? %>
    <h1>You are Logged In, <%= current_user.user_name %></h1>
    <%= button_to "Log Out", logout_path, method: :delete %>
<% else %>
    <%= button_to "Login", '/login', method: :get%>
<%end%>
